# Session Handoff: 92125479
Date: 2026-06-03 20:20 | Project: statusline-rs (cc-statusline) | Branch: master

> **✅ COMPLETED 2026-06-03 20:25** — the 6-band refinement described in
> "Remaining Work" was applied to BOTH SKILL.md copies by the follow-up session
> (8100509b). Boundary resolved as 85–89 / 80–84; lowest bands reference both
> `/next` and `/handoff:handoff`. Still uncommitted in this repo at time of writing.

## ⚠ READ THIS FIRST
This is a compressed summary, NOT ground truth.
Before modifying any code: re-read the actual files first, run tests,
verify your understanding. Treat this as orientation notes from a
colleague, not as a specification.

Note: the dead session nominally ran in `/home/ubuntu/projects/borry-raw-video`,
but **all its work targeted this repo** (`~/.claude/statusline-rs`) and the
installed skill copy at `~/.claude/skills/ctx-left/`. That's why the handoff
lives here.

## Original Plan / Task
(First user message, 2026-06-03 16:33, verbatim intent:)

> ho la skill /ctx-left che mi torna la % left di context. tutto apposto. il
> problema è che uso 1M di context e già dopo 80% le performance degradano
> (sarebbe il vecchio 200k context) e quando siamo al 70% la situazione è
> critica. al 60% left praticamente siamo quasi sicuri che ha allucinato.
> ora vorrei aggiornare la risposta della skill con queste indicazioni.

The pain point: the skill reported the raw % and Claude would answer with a
generic "Context attuale: 47% rimasto — c'è margine se preferisci continuare
qui…", which is exactly wrong on a 1M model where 47% left is deep in the
degradation zone.

## What Was Accomplished
1. Located both copies of the skill file:
   - `~/.claude/skills/ctx-left/SKILL.md` — **installed** copy (what Claude Code loads)
   - `~/.claude/statusline-rs/skill/ctx-left/SKILL.md` — **source** in this repo (survives reinstall via `install.sh`)
2. Added a new section **"## Verdict: soglie di degrado (modello 1M)"** to BOTH
   copies (+24 lines, uncommitted): a 4-band table on `ctx_left` (% remaining):
   - **> 80%** 🟢 sano
   - **71–80%** 🟡 degrado (at 80% left you've already burned ~200k ≈ one old full context)
   - **61–70%** 🟠 critico — plan the switch NOW
   - **≤ 60%** 🔴 allucinazioni — don't continue important work; launch `/next`
   Plus two hard rules: never say "c'è margine, continua pure" below 80%, and a
   worked example showing `ctx_left 47%` must produce a 🔴 "launch `/next`" verdict.
3. Verified the source-repo copy shows as a committable `M` in git (it still does).

No tests/linter involved — docs-only change to a SKILL.md.

## Key Decisions & Rationale
- **Update both copies, not just the installed one** — so the change takes
  effect immediately AND survives a reinstall (`install.sh` copies source →
  installed). Keep doing this for any further edits.
- **Thresholds are the user's empirical observations, not measurements** — the
  session noted this explicitly in its reply; the table is meant to be cheap to
  retune.
- **Recommendation is tied to the user's `/next` skill** (mega-prompt di
  continuità) as the action when dropping bands, phrased decisively ("lancia
  /next") rather than neutrally ("dimmi tu") from 🟠 down.

## ❌ FAILED APPROACHES — DO NOT RETRY
- None in the work itself — the session was short and linear (find files → edit
  both → verify git).
- The session **died on a spurious API error**, not context exhaustion: the
  user's final message (a plain list of numeric thresholds) triggered a false
  "Usage Policy" refusal (req_011CbgmAdaSQbpxE5C6qiNT1) at 18:00. There is
  nothing problematic in that message — just apply the edit it describes.
  Don't burn time investigating the error.

## Remaining Work  ← THE ACTUAL TASK
The user's last message (17:59) **refined the 4 bands into 6** and shifted the
framing from "when to restart" to "what task behavior is allowed per band".
This was NEVER applied. Replace the 4-band table/guidance in BOTH SKILL.md
copies with (user's words, verbatim):

> da 100 a 90 ok
> da 89 a 85 ok ma non iniziare task enormi
> da 85 a 80 non iniziare nuovi task
> da 79 a 75 valuta se interrompere i task oppure scriverlo nel /next: se sono
> piccoli e critical è meglio finirli subito.
> da 74 a 70 cerca di forzare l'handoff
> da 69 in giù: comunica nell'handoff che probabilmente il lavoro che è stato
> fatto ha allucinazioni o addirittura ha preso una strada potenzialmente
> diversa dal compito e quindi serve una verifica e un controllo umano sul lavoro

Sensible interpretation (note the user's bands overlap at 85 — resolve as):
| `ctx_left` | azione |
|-----------|--------|
| 90–100% | ok, piena capacità |
| 85–89% | ok, ma non iniziare task enormi |
| 80–84% | non iniziare nuovi task |
| 75–79% | valuta: interrompere i task o annotarli nel `/next`; se piccoli e critical, finirli subito |
| 70–74% | forza l'handoff |
| ≤ 69% | nell'handoff dichiara: il lavoro fatto probabilmente contiene allucinazioni o ha deviato dal compito → richiede verifica e controllo umano |

Implementation notes for the next session:
- The existing "## Verdict" section + 47% example need rewriting around the new
  bands (47% stays in the lowest band, but the message changes: it's now about
  flagging hallucination/divergence risk **in the handoff**, not just "restart").
- Keep the two hard rules' spirit (no "c'è margine" optimism; decisive
  recommendations as bands drop).
- "Handoff" in bands 70–74 / ≤69: the user has both `/next` (mega-prompt skill)
  and the `handoff` plugin (`/handoff:handoff`). The 4-band version only
  referenced `/next`. **Ask the user** (or mention both) — don't silently pick.
- Update BOTH copies and keep them in sync (they currently differ only in a
  pre-existing 2-line wording difference about install method, lines 15–16 —
  that difference is intentional/unrelated; don't "fix" it blindly).
- Then commit in this repo (the user was told the diff is committable; ask
  before committing per normal rules).

## Files Modified
From the session extract:
- `/home/ubuntu/.claude/skills/ctx-left/SKILL.md`: Read x1, Edit x1
- `/home/ubuntu/.claude/statusline-rs/skill/ctx-left/SKILL.md`: Read x1, Edit x1

Git (this repo, as of handoff creation):
```
git log --since="2026-06-03 16:30":  (no commits — change is uncommitted)
git diff --stat:
 skill/ctx-left/SKILL.md | 24 ++++++++++++++++++++++++
 1 file changed, 24 insertions(+)
git status --short:
 M skill/ctx-left/SKILL.md
```

## Architecture Notes
- **Two-copy skill layout**: source of truth is `skill/ctx-left/SKILL.md` in
  this repo (efebia-com/cc-statusline); `install.sh` / `cargo install` copy it
  to `~/.claude/skills/ctx-left/`. Any skill edit must touch both or it's lost
  on reinstall / inert until reinstall.
- The skill reads `~/.claude/ctx.db` (SQLite), upserted by the Rust statusline
  on every render. `ctx_left` output format:
  `ctx_left <pct>% · <count> render · ultimo campione <N>s fa`.
- The threshold semantics: `ctx_left` = % **remaining** (not used). All bands
  are expressed on remaining %.
- User's model context: 1M tokens, so 1% ≈ 10k tokens; 20% burned ≈ one full
  old 200k context.

## Git State
```
HEAD: e0f9da5aa03778c4c588c75efdd284582bb76723 (e0f9da5 feat: context tracking (ctx.db) + ctx-left skill + from-source installers)
Branch: master
Uncommitted: M skill/ctx-left/SKILL.md (+24 lines, the 4-band Verdict section)
```
If `skill/ctx-left/SKILL.md` no longer shows as modified or HEAD moved, re-read
the file before editing — someone may have committed or reverted the 4-band
version in the meantime.

## Verification Checklist
- [ ] Re-read both SKILL.md copies before editing (no tests/linter — docs-only)
- [ ] After editing: `diff ~/.claude/skills/ctx-left/SKILL.md ~/.claude/statusline-rs/skill/ctx-left/SKILL.md`
      → only the pre-existing lines 15–16 install-wording difference should remain
- [ ] `git -C ~/.claude/statusline-rs status --short` shows the source copy as `M`
- [ ] Confirm band-boundary interpretation (the 85 overlap) and the
      `/next`-vs-handoff wording with the user before finalizing

## Previous Handoffs
None in this project (directory created by this handoff). The handoffs under
`~/projects/borry-raw-video/.claude/handoffs/` belong to the borry project's
own work streams and are unrelated — not incorporated, not archived.
